===================================================
User


Attributes:

userId : UUI
name : String
email : String
passwordHash : String
role : Enum [NORMAL, AGENT, SUPER_ADMIN]
organizationId : UUID (nullable for super admin)
createdAt : DateTime


Methods:

authenticate()
resetPassword()

=====================================================


Agent
(Extends User where role = AGENT)

Attributes:

expertiseTags : List<String>
Relationships:
assignedToMany Tickets

SuperAdmin
(Extends User where role = SUPER_ADMIN)

Capabilities:

manageAllOrganizations()

manageUsers()

viewSystemLogs()

=====================================================

TicketComment
Attributes:
commentId : UUID
ticketId : UUID

authorId : UUID (User or Agent)

content : Text

createdAt : DateTime

AIAssistant
Attributes:

aiSessionId : UUID

query : Text

inferredIntent : String

generatedTicketId : UUID

Methods:

inferIntent()

createTicket()

TicketHistory
Attributes:

historyId : UUID

ticketId : UUID

statusFrom : Enum

statusTo : Enum

changedBy : UUID

timestamp : DateTime

ActivityLog
Attributes:

logId : UUID

actorId : UUID

action : String

targetType : String (Ticket, User, Org, etc.)

targetId : UUID

timestamp : DateTime
